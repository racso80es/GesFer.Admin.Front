# Validación

## Criterios de Aceptación
1. No debe existir ninguna línea en los archivos objetivo donde se pase `error` directamente como último parámetro a `console.error`.
2. El build de Next.js debe completarse exitosamente (`npm run build`).
3. Los tests deben ejecutarse correctamente (`npm run test`).
