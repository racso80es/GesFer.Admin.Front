# Implementación

- Analizado el contenido de `README.md` (principal) y `src/README.md`.
- Unificado ambos contenidos en el archivo `README.md` principal en la raíz del proyecto.
- Se agregó explícitamente el prefijo `src/` a los directorios explicados (`app/`, `components/`, etc.) para clarificar la estructura.
- Se añadió la instrucción de ejecutar comandos de desarrollo y configuración dentro del directorio `src/`.
- Eliminado el archivo `src/README.md`.