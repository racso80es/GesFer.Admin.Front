# Objectives

- Atender la instrucción Kaizen de la auditoría 2026-03-23.
- Modificar los route handlers de `api/admin/dashboard/summary`, `api/companies/[id]` y `api/companies` para asegurar el uso estricto de *type guards* y no loguear el objeto original del error.
- Validar que las extracciones de `languageOptions` en `company-form.tsx` y la refactorización de data fetching en `companies/page.tsx` ya se han completado.