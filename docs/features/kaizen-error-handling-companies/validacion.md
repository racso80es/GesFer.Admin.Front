# Validation

- TypeScript compilation passing cleanly (`npx tsc --noEmit`).
- Next.js static build passing cleanly (`npm run build`).
- `console.error` usages using strictly typed `message` variable.