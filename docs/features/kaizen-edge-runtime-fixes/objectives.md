# Objetivos
- Refactorizar `src/lib/config.ts` para eliminar la lectura de archivos con `fs` y `path`.
- Refactorizar `src/lib/api/server-fetch.ts` para arreglar el workaround de inyección de dependencia `https`.
