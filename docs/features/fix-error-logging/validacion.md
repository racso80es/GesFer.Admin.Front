# Validación
- Se verificó visualmente el código para comprobar que no se pasa el objeto `error` a `console.error`.
- El type guard es correcto según la auditoría.
- Se ha de validar con el compilador que no hay errores de sintaxis o tipo.
