# Plan
1. Analizar el código de `src/app/api/companies/[id]/route.ts` y `src/app/api/companies/route.ts`.
2. Sustituir `console.error(..., error)` por type guards seguros.
3. Verificar que compila y que las pruebas pasan de acuerdo a la Definition of Done.
