# Aclaraciones y Notas
- En base a la memoria, el sistema tiene `ADMIN_API_URL` mockeado en build.
- El issue afecta la forma en que los errores se muestran por consola en el backend de Next.js (Edge Runtime).
- No hay dependencias externas adicionales que agregar.
