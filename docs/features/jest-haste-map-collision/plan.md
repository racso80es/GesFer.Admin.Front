---
status: "active"
---

# Plan

1. Crear tarea automática en `docs/TASKS/ACTIVE/`.
2. Crear esta documentación en `docs/features/jest-haste-map-collision/`.
3. Ver que `src/jest.config.js` ya contenga `<rootDir>/.next/` en `modulePathIgnorePatterns` (Completado en fase de exploración).
4. Ejecutar tests para validar.
5. Finalizar tarea (mover a `DONE`), actualizar log y generar reporte final.
